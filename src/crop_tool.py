import os
import cv2

# Define paths relative to the project root
CONFIG_DIR = os.path.join("..", "config")
SLOTS_FILE = os.path.join(CONFIG_DIR, "slots.json")

def main():
    print("Starting crop tool...")
    # Add your OpenCV logic here to select slots
    pass

if __name__ == "__main__":
    main()
